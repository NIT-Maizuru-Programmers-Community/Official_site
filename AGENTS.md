# Repository instructions

このリポジトリを作業するCodex・Claude Code向けの共通ルールです。人向けの手順は [CONTRIBUTING.md](CONTRIBUTING.md)、依頼例と検証方法は [docs/agent-workflow.md](docs/agent-workflow.md) を参照してください。

## 最初に確認すること

- READMEとCONTRIBUTINGを読み、対象Issue・依頼の目的・完了条件を確認する。
- `git status --short` と現在のブランチを確認し、既存の未コミット変更を保持する。混在する場合は別ブランチ・worktreeを使う。
- 実装前に関連するコード・テストを読む。APIやパッケージの仕様が不明なら公式資料で確認する。
- 必要な環境は `pnpm check:env` で確認する。依存関係は `pnpm install --frozen-lockfile` で用意する。

## 実装方針

- Next.js App Router / React / TypeScript strict / Tailwind CSS。`output: 'export'` と7ページ・404の静的配信を維持する。
- `src/content/` のTypeScriptデータと `satisfies` による型検証を使う。下書きは公開一覧から除外する。
- Server Componentを基本とし、操作に必要な部分だけClient Componentにする。
- デザインは既存のCSS変数・コンポーネントを再利用する。依頼にない全面改修は避ける。
- API・DB・認証・CMS・新規依存は必要性を説明してから導入する。ロックファイルは意図した依存変更時だけ更新する。
- 画像・活動情報・問い合わせ先は確認済みの内容を使う。架空の実績や日時を公開データにしない。
- キーボード操作、フォーカス、代替テキスト、モバイル表示を維持する。
- 初心者がレビューできる小さな差分にし、変更理由を日本語で説明する。成果物に個人の会話口調・アカウント設定を持ち込まない。

## 検証

- 通常の編集後は `pnpm check:quick`。PR提出前は `pnpm check` を実行する。CIも同じcheckを実行する。
- `pnpm check` は整形確認・Lint・型チェック・テスト・ビルド・静的出力確認。最初の失敗で停止する。
- 書式修正は `pnpm format`。検証コマンドが自動修正するものと混同しない。
- UI変更時はブラウザで関連ページ、375px/768px/1440px、メニュー、キーボード操作を確認する。`pnpm check` だけで画面確認済みと報告しない。
- 不具合修正では再発を検知するテストを追加する。文書だけの変更は不要なアプリテストを増やさず、リンクとコマンドを確認する。
- テスト・Lint・型チェック・保護ルールを無効化して成功扱いにしない。未実施・環境制約・既存の失敗は分けて報告する。

## GitHubと権限

- `main` へ直接pushしない。作業ブランチからPRを作成し、チェックリストを事実に沿って記入する。
- 管理者の承認省略はレビュー用Rulesetのみ。必須CI `validate`、会話解決、force push・削除禁止は維持する。
- 依頼・セッションで許可された範囲で作業する。未許可のマージ・本番公開・権限変更・外部への投稿は行わない。
- 他人の変更を破棄しない。秘密情報・`.env`実値・依存パッケージ・ビルド出力をコミットしない。
- 指示ファイルは権限制御の代わりではない。実行環境の承認・サンドボックス設定を緩めない。

## 完了報告

何を変えたか、なぜ変えたか、実行した検証と結果、残る制限、PRがある場合はURLを簡潔に報告する。依頼がない限りモデル・APIへの有料自動実行や並列エージェントは追加しない。
